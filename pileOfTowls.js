const pileOfTowels = ["blue", "red", "blue", "red", "blue"];
const weeklyUsedTowels = [2, 1, 4, 2];

function sortThePile(pileOfTowels, weeklyUsedTowels) {
  console.log('pileOfTowels: ',pileOfTowels)
  console.log('weeklyUsedTowels: ',weeklyUsedTowels)

  // const sortedTowels = pileOfTowels.sort().reverse();
  // console.log('sortedTowels: ',sortedTowels)
  weeklyUsedTowels.foreach()

  return;
}

console.log('sorted: ',sortThePile(pileOfTowels, weeklyUsedTowels))

//node pileOfTowls.js